const mongoose = require('mongoose');
const Schema = mongoose.Schema;

const farmer_aggregator_emailSchema = new Schema({

});

module.exports = mongoose.model('farmer_agg_email', farmer_aggregator_emailSchema);